import { Container } from "react-bootstrap";

function GuessTheChatter() {
  document.body.className = "noOBS";

  return <Container className="GTCContainer"></Container>;
}

export default GuessTheChatter;
